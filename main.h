#pragma once
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int Key(const string&);
void Insert(HashTable&);
void TestFunction(const vector<Info*>&, const vector<string>&, HashTable*);